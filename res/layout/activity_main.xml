<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:id="@+id/linearLayout1"
    android:gravity="center"
    android:orientation="vertical" >

    <!-- Botão para escolha do autocarro -->

    <LinearLayout
        style="?android:attr/buttonBarStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center" >

        <EditText
            android:id="@+id/busLineButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:hint="Carreira"
            android:inputType="none"
            android:onClick="showBusDialog"
            android:focusable="false" 
            android:padding="15dp" />

        <EditText
            android:id="@+id/busIDButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:hint="ID Autocarro"
            android:inputType="number"
            android:padding="15dp" />

    </LinearLayout>

    <!-- Botão para dar início à viagem -->

    <Button
        android:id="@+id/startTripButton"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:onClick="startTrip"
        android:padding="20dp"
        android:text="@string/start" />
 <!-- Nome da função chamada por este botão -->

</LinearLayout>
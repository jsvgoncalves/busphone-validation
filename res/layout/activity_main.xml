<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity"
    android:id="@+id/linearLayout1"
    android:gravity="center"
    android:orientation="vertical" >

    <!-- Botão para escolha do autocarro -->

    <Button
        android:id="@+id/busNumberButton"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:onClick="showBusDialog"
        android:padding="20dp"
        android:text="@string/bus" />

    <!-- Botão para dar início à viagem -->
    <Button
        android:id="@+id/startTripButton"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:padding="20dp"
        android:text="@string/start"
        android:onClick="startTrip" /> <!-- Nome da função chamada por este botão --> 

</LinearLayout>